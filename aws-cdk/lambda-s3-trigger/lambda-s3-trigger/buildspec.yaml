version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"
  #secrets-manager:
     # key: secret-id:json-key:version-stage:version-id
     # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
     # - variable
     # - variable
  #git-credential-helper: yes
phases:
  install:
    runtime-versions:
      python: 3.11
      nodejs: 18
    #commands:
      # - command
      # - command
  pre_build:
    commands:
      - echo "Building Project..." 
      - ls
      - npm install -g aws-cdk
      - python -m venv .venv
      - . .venv/bin/activate
      - pip install -r requirements.txt
      - aws --version
      - aws configure set aws_access_key_id AKIAYZPMB5DAQLO5JF7A 
      - aws configure set aws_secret_access_key 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     
      - aws configure set region us-east-1
  build:
    commands:
      - cdk ls
      - cdk bootstrap -y
      # - cdk synth
      - cdk deploy --require-approval never
  #post_build:
    #commands:
      # - command
      # - command
#reports:
  #report-name-or-arn:
    #files:
      # - location
      # - location
    #base-directory: location
    #discard-paths: yes
    #file-format: JunitXml | CucumberJson
#artifacts:
  #files:
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths